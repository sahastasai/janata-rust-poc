{
  "name": "janata-rust-poc-benchmarks",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "bunx tsc --noEmit",
    "test": "bun test",
    "serve": "bun src/serve-static.ts",
    "env": "bun src/capture-environment.ts",
    "bundles": "bun src/analyze-bundles.ts",
    "pages": "bun src/measure-pages.ts",
    "api": "bun src/load-api.ts",
    "report": "bun src/render-report.ts",
    "all": "bun src/run-all.ts"
  },
  "devDependencies": {
    "@types/bun": "1.3.7",
    "playwright": "1.58.2",
    "typescript": "5.9.2"
  }
}
